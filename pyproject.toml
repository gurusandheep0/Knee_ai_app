[project]
name = "knee-ai-demonstrator"
version = "0.1.0"
description = "Local-first hackathon demonstrator for quantitative knee imaging"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
